const initialState = [
    {
        title: "My first item",
        id: 0
    }
];
export function quiz(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
